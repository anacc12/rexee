{
  "name": "proxy-server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npm install --legacy-peer-deps --force"
  },
  "engines": {
    "node": ">=16.x",
    "npm": ">=7.x"
  },
  "dependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.1"
  }
}
